# OctoWage — Docker Compose (produção)
# Uso: docker compose up -d

services:
  # === App FastAPI ===
  web:
    build: .
    container_name: octowage-web
    env_file:
      - .env
    expose:
      - "8000"
    restart: unless-stopped
    networks:
      - octowage-net

  # === Nginx (reverse proxy + SSL) ===
  nginx:
    image: nginx:alpine
    container_name: octowage-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./static:/app/static:ro
      - certbot-webroot:/var/www/certbot:ro
      - certbot-certs:/etc/letsencrypt:ro
    depends_on:
      - web
    restart: unless-stopped
    networks:
      - octowage-net

  # === Certbot (SSL Let's Encrypt) ===
  certbot:
    image: certbot/certbot
    container_name: octowage-certbot
    volumes:
      - certbot-webroot:/var/www/certbot
      - certbot-certs:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - octowage-net

  # === PostgreSQL (fase 2 — descomentar quando necessário) ===
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: octowage-db
  #   environment:
  #     POSTGRES_DB: octowage
  #     POSTGRES_USER: octowage
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mudar_em_producao}
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   restart: unless-stopped
  #   networks:
  #     - octowage-net

networks:
  octowage-net:
    driver: bridge

volumes:
  certbot-webroot:
  certbot-certs:
  # pgdata:
